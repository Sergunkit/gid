import { Canvas, Meta } from '@storybook/blocks';

import * as LoaderStories from './Loader.stories';

<Meta title="Компоненты/Loader" of={LoaderStories} />

# Loader

Компонент `Loader` отображает индикатор загрузки заданного размера
с возможностью индикации загрузки страницы.

<Canvas of={LoaderStories.Default} />
