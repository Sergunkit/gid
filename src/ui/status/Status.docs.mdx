import { Canvas, Meta } from '@storybook/blocks';

import * as StatusStories from './Status.stories';

<Meta title="Компоненты/Status" of={StatusStories} />

# Status

Компонент `Status` отображает статус различных объектов
с индикатором в виде точки и названием статуса.

<Canvas of={StatusStories.Active} />
