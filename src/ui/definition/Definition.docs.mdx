import { Canvas, Meta } from '@storybook/blocks';

import * as DefinitionStories from './Definition.stories';

<Meta title="Компоненты/Definition" of={DefinitionStories} />

# Definition

Компонент `Definition` отображает элемент списка описаний
в виде названия термина и его описания.

<Canvas of={DefinitionStories.Default} />
