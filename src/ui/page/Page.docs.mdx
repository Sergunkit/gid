import { Canvas, Meta } from '@storybook/blocks';

import * as PageStories from './Page.stories';

<Meta title="Компоненты/Page" of={PageStories} />

# Page

Компонент `Page` отображает базовую структуру страницы
приложения с её заголовком и содержимым.

<Canvas of={PageStories.Default} />
