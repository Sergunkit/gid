import { Canvas, Meta } from '@storybook/blocks';

import * as ContactStories from './Contact.stories';

<Meta title="Компоненты/Contact" of={ContactStories} />

# Contact

Компонент `Contact` отображает контактные данные
пользователя в виде иконки и значения.

<Canvas of={ContactStories.Default} />
