import { Canvas, Meta } from '@storybook/blocks';

import * as TextareaStories from './Textarea.stories';

<Meta title="Компоненты/Textarea" of={TextareaStories} />

# Textarea

Компонент `Textarea` отображает поле ввода текста
с дополнительными кнопками подтверждения и отмены.

<Canvas of={TextareaStories.Default} />
