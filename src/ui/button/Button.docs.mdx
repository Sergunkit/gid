import { Canvas, Meta } from '@storybook/blocks';

import * as ButtonStories from './Button.stories';

<Meta title="Компоненты/Button" of={ButtonStories} />

# Button

Компонент `Button` отображает кнопку, которая запускает определенное действие.

<Canvas of={ButtonStories.Download} />
